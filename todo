- add ram
add read and write [register to memory location (only first 256 addresses)]
add read write using write register [write to register and then only what register to write (all of addresses)]

- timer

- interrupt handle, start with timer



- make the rom bigger
    - support 2 arg memory location
    - 2 args for JMP
    - support for JMI instead of having the arg be the logic have the instruction be different
    JZ/JNZ/JC/JNC and the memory be first and second arg

